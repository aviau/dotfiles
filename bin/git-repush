#!/usr/bin/env bash

set -eo pipefail

main() {
    local default_remote=
    default_remote=$(git rev-parse --abbrev-ref origin/HEAD)
    git fetch --all
    git rebase ${default_remote}
    git push -u origin HEAD -f
}

main
