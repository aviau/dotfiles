#!/usr/bin/env bash

set -eo pipefail

export PATH="${HOME}/bin:/usr/local/bin:${PATH}"

pass show "$@" | awk 'BEGIN{ORS=""} {print; exit}'

osascript -e "display notification \"Copied password ${1} to clipboard\" with title \"Unix pass\""
